#include <stdio.h>
#include <stdint.h>


int main() {

	uint8_t Array1_size, Array2_size;

	printf("Enter no of element of Array-1:");
	scanf("%d", &Array1_size);
	printf("Enter no of element of Array-2:");
	scanf("%d", &Array2_size);
	if (Array1_size < 0 || Array2_size < 0) {
		printf("Number of element cannot be negative\n");
		return 0;
	}
	uint8_t Array1[Array1_size];



}

void swap(uint8_t array1, uint8_t array2) {



}